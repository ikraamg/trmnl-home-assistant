#!/command/with-contenv bash
# shellcheck shell=bash# Sidekiq graceful shutdown

echo "[sidekiq] Service stopping (exit code: $1, signal: $2)"

# Sidekiq handles SIGTERM gracefully (finishes current jobs)
exit 0
