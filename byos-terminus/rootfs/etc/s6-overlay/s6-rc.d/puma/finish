#!/command/with-contenv bash
# shellcheck shell=bash# Puma graceful shutdown

echo "[puma] Service stopping (exit code: $1, signal: $2)"

# Puma handles SIGTERM gracefully, no extra action needed
exit 0
